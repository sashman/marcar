# Migration `20200416151250`

This migration has been generated by sashman at 4/16/2020, 3:12:50 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
    "email" text  NOT NULL ,
    "id" SERIAL,
    "name" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Team" (
    "id" SERIAL,
    "matchId" integer   ,
    "name" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Participant" (
    "id" SERIAL,
    "matchId" integer  NOT NULL ,
    "teamId" integer   ,
    "userId" integer  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Owner" (
    "id" SERIAL,
    "matchId" integer   ,
    "userId" integer  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Match" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" SERIAL,
    "scheduledAt" timestamp(3)  NOT NULL ,
    "updatedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "Owner_matchId" ON "public"."Owner"("matchId")

ALTER TABLE "public"."Team" ADD FOREIGN KEY ("matchId")REFERENCES "public"."Match"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Participant" ADD FOREIGN KEY ("matchId")REFERENCES "public"."Match"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Participant" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Participant" ADD FOREIGN KEY ("teamId")REFERENCES "public"."Team"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Owner" ADD FOREIGN KEY ("matchId")REFERENCES "public"."Match"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Owner" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200415225742..20200416151250
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -14,14 +14,24 @@
   participants Participant[]
   owner        Owner[]
 }
+model Team {
+  id           Int           @default(autoincrement()) @id
+  name         String
+  participants Participant[]
+  match        Match?        @relation(fields: [matchId], references: [id])
+  matchId      Int?
+}
+
 model Participant {
   id      Int   @default(autoincrement()) @id
   match   Match @relation(fields: [matchId], references: [id])
   matchId Int
   user    User  @relation(fields: [userId], references: [id])
   userId  Int
+  team    Team? @relation(fields: [teamId], references: [id])
+  teamId  Int?
 }
 model Owner {
   id      Int    @default(autoincrement()) @id
@@ -37,5 +47,6 @@
   updatedAt    DateTime      @default(now())
   participants Participant[]
   owner        Owner
   scheduledAt  DateTime
+  teams        Team[]
 }
```


